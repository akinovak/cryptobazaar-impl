# Examples for cipher-bazaar

This folder contains runnable examples for the `cipher-bazaar` crate.

## Running the simple end-to-end example

```bash
cargo run --example simple_e2e
```

You can add more examples in this folder. Each file should be a standalone Rust program demonstrating usage of the crate.
